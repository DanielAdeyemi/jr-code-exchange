@{
  Layout = "_Layout";
}

@using CodeExchange.ViewModels

@model RegisterViewModel

<h2>Register a new user</h2>
<hr />

@if(ViewBag.count > 0)
{
  <p> Email is unavailable. Please try again with a different email! </p>
}


@using (Html.BeginForm("Register", "Account", FormMethod.Post))
{
    @Html.LabelFor(user => user.Email)
    @Html.TextBoxFor(user=> user.Email, new {@required="required"})

    @Html.LabelFor(user=> user.Password)
      @Html.PasswordFor(user=> user.Password, new {@required="required"})

    @Html.LabelFor(user=> user.ConfirmPassword)
    @Html.PasswordFor(user=> user.ConfirmPassword, new {@required="required"})

    <input type="submit" value="Register" class="btn btn-success" />
}

<p>@Html.ActionLink("Login", "Login")</p>
<p>@Html.ActionLink("Back - Home", "Index", "Home")</p>